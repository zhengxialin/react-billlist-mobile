# React + Vite

This project is using EChart and Ant-Design：
> pnpm install antd-mobile echarts

To run the project, you need to start the mocking server first:
> pnpm run server

Then start another terminal: 
> pnpm run dev

To test the project in mobile mode:
Ctrl + Shift + i and then click "dimensions" to choose a mobile type
